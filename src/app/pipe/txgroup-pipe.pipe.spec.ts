import { TxgroupPipePipe } from './txgroup-pipe.pipe';

describe('TxgroupPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TxgroupPipePipe();
    expect(pipe).toBeTruthy();
  });
});
